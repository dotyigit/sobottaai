"use client";

import { RecordingBar } from "@/components/recording-bar";

export default function RecordingBarPage() {
  return (
    <div className="flex h-screen w-screen items-center justify-center bg-zinc-900">
      <RecordingBar />
    </div>
  );
}
